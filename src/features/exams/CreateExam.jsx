import { Button } from "antd";

const CreateExam = () => {

  return (
    <span>
        <Button className="create" type="primary">
          Add New Exam
        </Button>
    </span>
  );
};

export default CreateExam;
