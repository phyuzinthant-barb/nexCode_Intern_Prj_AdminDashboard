import { Button } from "antd";

const CreateCourses = () => {

  return (
    <span>
        <Button className="create" type="primary">
          Add New Course
        </Button>
    </span>
  );
};

export default CreateCourses;
